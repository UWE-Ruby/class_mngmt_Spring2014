<%= form_for(@student) do |f| %>
  <% if @student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
      <% @student.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :full_name %><br>
    <%= f.text_field :full_name %>
  </div>
  <div class="field">
    <%= f.label :age %><br>
    <%= f.number_field :age %>
  </div>
  <div class="field">
    <%= f.label :bio %><br>
    <%= f.text_area :bio %>
  </div>
  <div class="field">
    <%= f.label :profession %><br>
    <%= f.text_field :profession %>
  </div>
  <div class="field">
    <%= f.label :favorite_ice_cream_flavor %><br>
    <%= f.text_field :favorite_ice_cream_flavor %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>

<p><h1> Course Fields </h1>
  <%= f.fields_for :courses do |course_form| %>
    <div class="field">
      <%= course_form.label :title %><br>
      <%= course_form.text_field :title %>
    </div>
  <% end %></p>

    <%= f.collection_select(:course_ids, Course.all, :id, :title, {selected: @student.course_ids, include_blank: false},{ multiple: true }) %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
